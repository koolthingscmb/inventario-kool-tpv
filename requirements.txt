customtkinter==5.2.2
darkdetect==0.8.0
packaging==26.0
pillow==12.1.0

# Nota: `setuptools` está en el entorno virtual, pero normalmente no se incluye en `requirements.txt`.

reportlab==4.4.10
python-barcode==0.16.1
pywin32>=311; platform_system=='Windows'
